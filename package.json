{
  "name": "root",
  "private": true,
  "scripts": {
    "components:build": "lerna run build --scope=@monorepo/components"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
